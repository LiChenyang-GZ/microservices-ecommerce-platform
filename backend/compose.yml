services:
  rabbitmq:
    container_name: 'rabbitmq-5348'
    image: 'rabbitmq:3.13-management'
    environment:
      - 'RABBITMQ_DEFAULT_PASS=admin'
      - 'RABBITMQ_DEFAULT_USER=admin'
    ports:
      - '5672:5672'
      - '15672:15672'

# rabbitmq-plugins enable rabbitmq_managemen